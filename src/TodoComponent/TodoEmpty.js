import React from "react";

function TodoEmpty() {
  return (
    <div className="TodoEmpty">
      {/* if SearchedText Exists then show span */}
      <span>EMPTY</span>
    </div>
  );
}

export { TodoEmpty };
